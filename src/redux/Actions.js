/* export const addProduct = (dispatch, id, name, image, price) => {
    return () => {
        const productData = {
            id,
            name,
            image,
            price,
        }
        dispatch({ type: 'ADD_TO_CART', payload: productData })
    }
} */